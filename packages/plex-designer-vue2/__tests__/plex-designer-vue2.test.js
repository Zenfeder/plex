'use strict';

const plexDesignerVue2 = require('..');
const assert = require('assert').strict;

assert.strictEqual(plexDesignerVue2(), 'Hello from plexDesignerVue2');
console.info('plexDesignerVue2 tests passed');
